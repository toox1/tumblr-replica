	<h1>Make something</h1>
	<%= form_for :post, url: posts_path do |f|%>
	<h2>
		<%= f.label :title %><br>
		<%= f.text_field :title%>
	</h2>

	<p>
		<%= f.label :body %><br>
		<%= f.text_area :body %>
	</p>
	<br>
		<%= f.submit %>

		<% end %>